master:
  script:
    - apt-get update -qy
    - apt-get install -qy python-numpy python-scipy pyflakes
    - pwd
    - echo $HOME
    - echo $PWD
    - echo $PYTHONPATH
    - PATH=$PATH:$PWD/tools PYTHONPATH=$PWD python -m ase.test
    - PYTHONPATH=$PWD python ase/test/__init__.py
