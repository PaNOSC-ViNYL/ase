master:
  script:
    - apt-get update -qy
    - apt-get install -qy python-numpy python-scipy
    - pwd
    - echo $HOME
    - echo $PWD
    - echo $PYTHONPATH
    - PATH=$PATH:$PWD/tools PYTHONPATH=$PYTHONPATH:$PWD python setup.py test
